spring:
  application:
    name: personal-data-vault
  datasource:
    url: jdbc:postgresql://localhost:5432/vaultdb
    username: vault_user
    password: strongpassword
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
